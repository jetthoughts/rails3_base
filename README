RECIPES=haml,rspec,cucumber,guard,mongoid,spork,devise,json,cleanup,git
FEATURES=haml,rspec,cucumber,guard,mongoid,spork,devise,jbuilder

commands(for testing purpose):

generating template(from rails_composer folder):
rake print --silent RECIPES=haml,rspec,cucumber,guard,mongoid,spork,devise,json,cleanup,git > ../rails3_base/rails3_base.rb

rails project from template:
FEATURES=haml,rspec,cucumber,guard,mongoid,spork,devise,jbuilder rails new myapp -m rails3_base/rails3_base.rb -O -T

Expected feature list:

Database:
datacleaner - in rspec recipe
mongodb - in mongoid recipe

Testing:
guard - in guard recipe(with guard-spork)
spork - in spork recipe
capybara - in capybara recipe
rspec - in rspec recipe
fabrication - ??

Auth:
devise - in devise recipe
cancan

Images and other attachments:
carrierwave
rmagick

Development:
haml - in haml recipe
inherited_resources
jsbuilder - ?? jbuilder?
has_scope
responders
simple_form
show_for

Pagination:
kaminari

Styles:
bootstrap - in html5 recipe (defaults not working now)

ToDo:

gem 'bootstrap-sass', '~> 1.4.3'

gem 'devise_invitable', '~> 0.6.1'
gem 'cancan', '~> 1.6.7'

gem 'inherited_resources'
gem 'has_scope'
gem 'responders'
gem 'show_for', git: 'git://github.com/plataformatec/show_for.git'
gem 'simple_form', git: 'git://github.com/plataformatec/simple_form.git', ref: '95f711366184cf38ebf9a571387ba5006114a40a'
gem 'pacecar'

gem 'pg', '~> 0.12.2'
gem 'state_machine'
gem 'kaminari', '~> 0.13.0'
gem 'carrierwave', '~> 0.5.8'
gem 'fog', '~> 1.1.2'
gem 'rmagick', '~> 2.13.1'

gem 'yajl-ruby', '~> 1.1.0'
gem 'newrelic_rpm'
gem 'tddium', '~> 1.0.8'
gem 'sunspot_rails', '1.3.0'

group :development, :test do

  gem 'sunspot_solr', '1.3.0'
  gem 'ruby-debug19', :require => 'ruby-debug'
end

group :production do
  gem 'unicorn', '~> 4.1.1'
end

group :test do
  gem 'simplecov', '~> 0.5.4', :require => false
  gem 'factory_girl_rails', '~> 1.5.0'
  gem 'capybara-webkit', '~> 0.7.2'
  gem 'sunspot_test', '0.4.0'
  gem 'shoulda-matchers', '1.0.0'

end

group :development do
  gem 'heroku', '~> 2.18.1'
  gem 'hirb'
  gem 'wirble'
end
